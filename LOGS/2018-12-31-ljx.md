# 12-31 一些发现
1. 在`settings.py`中如果设置`DEBUG = True`, 则输入地址时可以请求到图片，如在浏览器网址输入栏中输入`localhost:8000/polls`，可以请求到图片，而当`DEBUG = False`时，在`ALLOWED_HOSTS`中写上localhost，同样在浏览器中发出请求，则请求不到图片。
使用`127.0.0.1`则都可以请求到。
2. 忽略上面那一条，是测试失误，都可以请求到图片！
